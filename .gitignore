# Dependencies
node_modules/

# Local env — NEVER commit .env (contains API keys)
.env
.env.*
!.env.example

# TTS audio cache (regenerated on demand). Now lives in /tmp on
# serverless and under .cache/ locally; the source module was
# inlined into server.js for Vercel-bundler compatibility.
.cache/

# Vercel
.vercel/

# Puku CLI local config (per-machine, not for source control)
.puku/

# Local env / OS
.DS_Store
*.log
