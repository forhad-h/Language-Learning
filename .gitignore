# Dependencies
node_modules/

# Local env — NEVER commit .env (contains API keys)
.env
.env.*
!.env.example

# TTS audio cache (regenerated on demand)
# Source files in server/cache/ are SHIPPED — only ignore the runtime
# artifacts (.bin/.json entries produced at runtime).
server/cache/**/*.bin
server/cache/**/*.json
.cache/

# Vercel
.vercel/

# Local env / OS
.DS_Store
*.log
